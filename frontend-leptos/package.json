{
  "name": "frontend-leptos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:vercel": "env -u NO_COLOR SHOKEN_WEBAPI_URL=https://shoken-backend.fly.dev trunk build --release --dist dist-vercel",
    "e2e:vercel": "env -u NO_COLOR playwright test --config playwright.vercel.config.ts"
  },
  "dependencies": {
    "@playwright/test": "^1.63.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
