The `*-input.json` files contain shared calculation inputs. The matching
`*-expected.json` files contain outputs produced by the existing React
calculation functions. Both the React and Rust test suites read these files.
